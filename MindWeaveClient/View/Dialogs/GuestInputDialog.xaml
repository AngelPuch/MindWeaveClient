<Window x:Class="MindWeaveClient.View.Dialogs.GuestInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MindWeaveClient.View.Dialogs"
        xmlns:lang="clr-namespace:MindWeaveClient.Properties.Langs"
        mc:Ignorable="d"
        Title="{x:Static lang:Lang.GuestInvitePromptTitle}" Height="200" Width="400"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Static lang:Lang.GuestInvitePromptMessage}" TextWrapping="Wrap" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" x:Name="EmailTextBox" 
                 Style="{StaticResource AuthInputBoxStyle}" 
                 Height="35"
                 MaxLength="45"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static lang:Lang.GuestInviteBtnSend}" Style="{StaticResource PrimaryButtonStyle}" Width="100" Height="35" Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button Content="{x:Static lang:Lang.GlobalBtnCancel}" Style="{StaticResource SecondaryButtonStyle}" Width="100" Height="35" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>