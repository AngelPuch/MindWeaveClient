<UserControl x:Class="MindWeaveClient.View.Game.PuzzlePieceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MindWeaveClient.View.Game"
             xmlns:puzzle="clr-namespace:MindWeaveClient.ViewModel.Puzzle"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=puzzle:PuzzlePieceViewModel}"
             Width="{Binding Width}" 
             Height="{Binding Height}">

    <Image Source="{Binding PieceImage}" ToolTip="{Binding PieceId}">
        <Image.Style>
            <Style TargetType="Image">
                <Setter Property="Opacity" Value="1.0"/>
                <Setter Property="Effect" Value="{x:Null}"/>

                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsPlaced}" Value="True">
                        <Setter Property="IsEnabled" Value="False"/>
                        <Setter Property="Opacity" Value="1.0"/>
                    </DataTrigger>

                    <DataTrigger Binding="{Binding IsHeldByOther}" Value="True">
                        <Setter Property="IsEnabled" Value="False"/>
                        <Setter Property="Opacity" Value="0.3"/>
                        <Setter Property="ToolTip" Value="Held by another player..."/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Image.Style>
    </Image>
</UserControl>
